spring:
  application:
    name: EventClean

  datasource:
    url: jdbc:postgresql://localhost:5431/eventclean
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true